# todo check if url parameters are really necessary
spring.datasource.url=jdbc:postgresql://localhost:7004/payment-history?\
  autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=admin
spring.datasource.password=admin

delegation.hostname=localhost

scaling.interval.small=5
scaling.interval.medium=10
scaling.interval.large=15

active-mq.broker-url=tcp://localhost:61616
