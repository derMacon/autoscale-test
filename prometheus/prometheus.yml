global:
  scrape_interval: 10s
  scrape_timeout: 10s

rule_files:
  - alert.yml

scrape_configs:
  - job_name: services
    metrics_path: /metrics
    static_configs:
      - targets:
          - 'prometheus:9090'
          - 'idonotexists:564'
          - 'helloworld:9090'
          - 'localhost:9090'

alerting:
  alertmanagers:
    - static_configs:
      - targets:
        - alertmanager:9093